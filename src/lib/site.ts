/**
 * The site's public origin, used for canonical URLs, the sitemap, robots.txt,
 * OpenGraph URLs and the contact form's post-submit redirect.
 *
 * NOTE: this is still a placeholder — the real domain has not been confirmed.
 * Changing it here updates every one of those places at once.
 */
export const SITE_URL = "https://travo.co";
